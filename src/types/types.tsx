export type City = {
    title: string;
    lat: number;
    lng: number;
    zoom: number;
  };

export type CityName =
  | 'Paris'
  | 'Cologne'
  | 'Brussels'
  | 'Amsterdam'
  | 'Hamburg'
  | 'Dusseldorf';

export type Point = {
    title: string;
    lat: number;
    lng: number;
}
